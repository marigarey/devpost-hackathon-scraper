[project]
name = "devpost-hackathon-scraper"
version = "0.1.0"
description = "Collects the project submissions from a specific hackathon"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.33.1",
    "playwright>=1.40.0",
]
